<resources>
    <!-- Base application theme. -->
    <style name="Theme.MagicRampageCompanion" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/grey</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/grey</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentNavigation">true</item> //Bottom Buttons are translucent
        <item name="android:windowTranslucentStatus">true</item> //When swiping down Status bar, it is translucent
        <item name="android:fitsSystemWindows">true</item> //No idea what this does, lol
        <item name="android:windowFullscreen">true</item>  //Remove Status Bar
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast">true</item>

    </style>

</resources>